{
  "name": "playwright-js-automation",
  "version": "1.0.0",
  "description": "Automation framework for web applications using Playwright",
  "main": "index.js",
  "scripts": {
    "clean:allure": "rimraf ./allure-results",
    "clean:test-results": "rimraf ./test-results",
    "setup:allure": "node allure-setup.js",
    "pretest": "npm run clean:allure && npm run clean:test-results && npm run setup:allure",
    "test": "playwright test",
    "test:e2e": "playwright test tests/e2e",
    "test:api": "playwright test tests/api",
    "allure:generate": "allure generate ./allure-results --clean -o ./allure-report",
    "allure:open": "allure open ./allure-report",
    "test:allure": "npm run clean:allure && npm run clean:test-results && npm run setup:allure && playwright test && npm run allure:generate && npm run allure:open"
  },
  "keywords": [
    "playwright",
    "automation",
    "testing",
    "e2e",
    "api"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "playwright": "^1.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "allure-commandline": "^2.34.1",
    "allure-playwright": "^3.4.2",
    "eslint": "^7.0.0",
    "prettier": "^2.0.0",
    "rimraf": "^6.1.2"
  }
}